# Issue #44: shared Svelte component adoption

| Behavior / invariant | Trigger | Positive evidence | Negative / failure evidence | Actor / context | Test level | Validation |
| --- | --- | --- | --- | --- | --- | --- |
| Overview and Billing render the published subscription summary and threshold presentation | Authenticated tenant visits `/app` or `/app/billing` | The shared summary and thresholds mount from the entitlement resolution returned by the existing server-authorized load | A user without the route permission is still rejected by the unchanged server load | Tenant member with the applicable read permission | Browser E2E and route authorization regression | `pnpm --filter @happyvertical/smrt-saas-web test:e2e`; existing server tests/typecheck |
| Billing plan selection preserves the existing checkout action | A permitted billing manager selects a plan | The shared `PlanPicker` submits an alternative public plan id to the existing checkout action | Selecting the current plan is a no-op; missing plan id remains a 400; the existing action keeps its permission check | Billing manager in the active tenant | Unit, server action regression, and browser E2E | Web test suite; `pnpm check` |
| Usage thresholds use the published threshold component | A permitted member visits `/app/usage` | The component receives the unchanged entitlement `ThresholdEvaluation[]` | Missing usage permission remains denied by the unchanged load | Tenant member with usage read permission | Browser E2E and route authorization regression | `pnpm --filter @happyvertical/smrt-saas-web test:e2e`; `pnpm check` |
| Public access-request fields use provider-free shared controls | A visitor opens `/request-access` and submits the form | Email, name, company, and message fields retain their native names and submitted values | Existing server-side malformed-input and rate-limit handling remains authoritative | Unauthenticated visitor | Browser regression and route-action regression | Web test suite; `pnpm check` |
| Starter does not retain a local generic billing, plan, or usage widget | Static component inventory | Only the public subscriptions components provide those surfaces | Any future new component must satisfy the AGENTS catalog/reuse rule | Maintainer | Source inventory and typecheck | `rg` inventory; `pnpm typecheck` |

No persistence, tenancy, external-provider, or retry behavior changes: existing server loads and actions retain their authorization and Stripe boundaries.
