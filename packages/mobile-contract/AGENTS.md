# @happyvertical/smrt-saas-mobile-contract

This package defines the narrow DTOs mobile clients consume. Mobile must not read raw SMRT manifests at runtime.

## Validation

```sh
pnpm --filter @happyvertical/smrt-saas-mobile-contract generate
pnpm --filter @happyvertical/smrt-saas-mobile-contract typecheck
```
