name: Setup environment
description: Install Node, pnpm, and workspace dependencies.

runs:
  using: composite
  steps:
    - name: Enable pnpm for setup-node cache
      shell: bash
      run: |
        corepack enable
        corepack prepare pnpm@10.33.0 --activate

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: "24"
        cache: "pnpm"

    - name: Activate pnpm
      shell: bash
      run: |
        corepack prepare pnpm@10.33.0 --activate

    - name: Configure GitHub Packages
      shell: bash
      env:
        GH_PACKAGES_TOKEN: ${{ env.GH_PACKAGES_TOKEN }}
        GITHUB_PACKAGES_TOKEN: ${{ env.GITHUB_PACKAGES_TOKEN }}
        NODE_AUTH_TOKEN: ${{ env.NODE_AUTH_TOKEN }}
        GITHUB_TOKEN: ${{ github.token }}
      run: |
        token="${GH_PACKAGES_TOKEN:-${GITHUB_PACKAGES_TOKEN:-${NODE_AUTH_TOKEN:-$GITHUB_TOKEN}}}"
        echo "@happyvertical:registry=https://npm.pkg.github.com" >> "$HOME/.npmrc"
        echo "//npm.pkg.github.com/:_authToken=${token}" >> "$HOME/.npmrc"

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
      env:
        NODE_AUTH_TOKEN: ${{ env.NODE_AUTH_TOKEN || env.GH_PACKAGES_TOKEN || env.GITHUB_PACKAGES_TOKEN || github.token }}
        GH_PACKAGES_TOKEN: ${{ env.GH_PACKAGES_TOKEN || env.GITHUB_PACKAGES_TOKEN || env.NODE_AUTH_TOKEN || github.token }}
        GITHUB_PACKAGES_TOKEN: ${{ env.GITHUB_PACKAGES_TOKEN || env.GH_PACKAGES_TOKEN || env.NODE_AUTH_TOKEN || github.token }}
